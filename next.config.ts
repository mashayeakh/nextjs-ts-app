import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactCompiler:true, // enable the new React 18 compiler that improves performance and reduces bundle size
  /* config options here */
};

export default nextConfig;
